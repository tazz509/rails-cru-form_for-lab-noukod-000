<h1><%= @song.name %></h1>
<p>Genre: <%= link_to @song.genre.name, genre_path(@song.genre.id) if @song.genre %></p>
<p>Artist: <%= link_to @song.artist.name, artist_path(@song.artist.id) if @song.artist %></p>
